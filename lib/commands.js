const fs = require('fs');
const path = require('path');

module.exports = {

	
	load: function(filePath) {
		return require(filePath);
	},

	merge: function(commands) {
		
	},

	select: function(value) {
		
	},

	import: function(value) {
		
	},

	export: function(value) {
		
	},

};